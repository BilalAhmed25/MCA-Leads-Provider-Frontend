import BlogSection from '../components/BlogSection';

const Blog = () => {
    return (
        <main style={{ paddingTop: '60px', backgroundColor: 'var(--white)' }}>
            <BlogSection />
        </main>
    );
};

export default Blog;
